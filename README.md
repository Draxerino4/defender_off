# defender_off
Contains a script to deactivate Windows Defender.


How to use:

In Powershell (with Administrator privileges) run: defender.ps1


WARNING: Use at your OWN risk. You can actually read all the code inside the file, so that you can see that there is no malware, it simply creates the necessary keys in the registry to turn off Windows Defender.
